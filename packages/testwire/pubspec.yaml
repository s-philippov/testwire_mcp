name: testwire
description: Step-based test execution framework for Flutter integration tests with AI agent control.
version: 0.1.0
resolution: workspace
environment:
  sdk: ^3.10.0
dependencies:
  meta: ^1.16.0
  testwire_protocol:
dev_dependencies:
  lints: ^6.0.0
