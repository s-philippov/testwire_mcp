## 0.1.0

- Initial release.
- `step()` API for defining test steps with descriptions and context.
- `TestSession` for managing step execution and agent communication.
- `runTestLoop()` — hot-reload-aware test execution loop.
- VM service extension registration (`registerTestwireExtensions()`).
